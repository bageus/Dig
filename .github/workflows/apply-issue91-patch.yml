name: Apply issue 91 implementation patch

on:
  pull_request:
    branches:
      - main

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap disabled; files are published directly."
