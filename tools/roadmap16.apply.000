diff -ruN '--exclude=__pycache__' dig-current-test/.github/workflows/quality.yml dig-latest/.github/workflows/quality.yml
--- a/.github/workflows/quality.yml	2026-07-29 00:36:40.000000000 +0000
+++ b/.github/workflows/quality.yml	2026-07-29 07:37:54.625519556 +0000
@@ -37,6 +37,9 @@
           set -o pipefail
           python tools/quality/check_unity_source_contracts.py 2>&1 | tee unity-source-contracts.log
 
+      - name: Validate Unity runtime evidence tooling
+        run: python tools/quality/validate_unity_runtime_evidence.py --self-test
+
       - name: Validate Unity native field initialization
         run: python tools/quality/check_unity_native_field_initializers.py
 
diff -ruN '--exclude=__pycache__' dig-current-test/.github/workflows/unity-playmode.yml dig-latest/.github/workflows/unity-playmode.yml
--- a/.github/workflows/unity-playmode.yml	2026-07-29 00:36:40.000000000 +0000
+++ b/.github/workflows/unity-playmode.yml	2026-07-29 07:37:54.177519548 +0000
@@ -7,6 +7,7 @@
       - ".github/workflows/unity-playmode.yml"
       - "src/**"
       - "unity/**"
+      - "tools/quality/validate_unity_runtime_evidence.py"
   push:
     branches:
       - main
@@ -14,16 +15,19 @@
       - ".github/workflows/unity-playmode.yml"
       - "src/**"
       - "unity/**"
+      - "tools/quality/validate_unity_runtime_evidence.py"
 
 permissions:
   contents: read
   checks: write
 
 jobs:
-  playmode:
-    name: Unity 6000.0.71f1 Play Mode
+  unity-tests:
+    name: Unity 6000.0.71f1 EditMode + PlayMode
     runs-on: ubuntu-latest
     timeout-minutes: 60
+    env:
+      UNITY_VERSION: 6000.0.71f1
 
     steps:
       - name: Checkout
@@ -32,6 +36,11 @@
           lfs: true
           show-progress: false
 
+      - name: Set up Python
+        uses: actions/setup-python@v5
+        with:
+          python-version: "3.12"
+
       - name: Resolve Unity activation
         id: activation
         env:
@@ -56,7 +65,7 @@
           if [[ "${configured}" == "true" ]]; then
             echo "Unity activation inputs are configured."
           else
-            echo "::warning title=Unity Play Mode skipped::${reason}. Add repository Actions secrets to produce licensed runtime evidence."
+            echo "::warning title=Unity runtime tests blocked::${reason}. Add repository Actions secrets to produce licensed runtime evidence."
           fi
 
       - name: Cache Unity Library
@@ -68,9 +77,9 @@
           restore-keys: |
             unity-library-${{ runner.os }}-6000.0.71f1-
 
-      - name: Run Play Mode tests
+      - name: Run Unity EditMode and PlayMode tests
         if: steps.activation.outputs.configured == 'true'
-        id: playmode
+        id: unity_tests
         uses: game-ci/unity-test-runner@v4
         env:
           UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
@@ -80,27 +89,53 @@
         with:
           projectPath: unity/Dig.Unity
           unityVersion: 6000.0.71f1
-          testMode: PlayMode
-          artifactsPath: artifacts/playmode
+          testMode: All
+          artifactsPath: artifacts/unity-tests
           githubToken: ${{ secrets.GITHUB_TOKEN }}
-          checkName: Dig Unity Play Mode
+          checkName: Dig Unity EditMode + PlayMode
 
-      - name: Record skipped runtime evidence
+      - name: Validate executed Unity runtime evidence
+        if: always() && steps.activation.outputs.configured == 'true'
+        shell: bash
+        run: |
+          python tools/quality/validate_unity_runtime_evidence.py \
+            --artifacts-dir artifacts/unity-tests \
+            --manifest artifacts/unity-runtime-evidence.json \
+            --required-test "EditMode=Dig.Unity.EditModeTests.UnityProjectEditModeTests.Main_scene_is_registered_with_runtime_bootstrap" \
+            --required-test "PlayMode=Dig.Unity.PlayModeTests.RepresentativeSceneConsolePlayModeTests.Main_scene_bootstraps_without_console_errors" \
+            --required-log "runtime/representative-scene.log"
+
+      - name: Record blocked runtime evidence
         if: steps.activation.outputs.configured != 'true'
         shell: bash
         run: |
+          python tools/quality/validate_unity_runtime_evidence.py \
+            --artifacts-dir artifacts/unity-tests \
+            --manifest artifacts/unity-runtime-evidence.json \
+            --required-test "EditMode=Dig.Unity.EditModeTests.UnityProjectEditModeTests.Main_scene_is_registered_with_runtime_bootstrap" \
+            --required-test "PlayMode=Dig.Unity.PlayModeTests.RepresentativeSceneConsolePlayModeTests.Main_scene_bootstraps_without_console_errors" \
+            --blocked-reason "${{ steps.activation.outputs.reason }}"
           {
-            echo "## Unity Play Mode not executed"
+            echo "## Unity runtime evidence blocked"
             echo
             echo "${{ steps.activation.outputs.reason }}."
-            echo "The PR check is not a test failure; licensed Play Mode evidence remains pending."
+            echo "The workflow conclusion alone is not verification. Inspect the unity-runtime-evidence artifact."
           } >> "${GITHUB_STEP_SUMMARY}"
 
-      - name: Upload Play Mode results
+      - name: Upload Unity test results
         if: always() && steps.activation.outputs.configured == 'true'
         uses: actions/upload-artifact@v4
         with:
-          name: unity-playmode-results
-          path: artifacts/playmode
-          if-no-files-found: warn
+          name: unity-editmode-playmode-results
+          path: artifacts/unity-tests
+          if-no-files-found: error
           retention-days: 14
+
+      - name: Upload Unity runtime evidence manifest
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: unity-runtime-evidence
+          path: artifacts/unity-runtime-evidence.json
+          if-no-files-found: error
+          retention-days: 30
diff -ruN '--exclude=__pycache__' dig-current-test/docs/ROADMAP.md dig-latest/docs/ROADMAP.md
--- a/docs/ROADMAP.md	2026-07-29 00:36:40.000000000 +0000
+++ b/docs/ROADMAP.md	2026-07-29 07:42:11.669524569 +0000
@@ -1,5 +1,7 @@
 # Roadmap проекта Dig
 
+Статус базовой roadmap: `IMPLEMENTED` после закрытия [#16](https://github.com/bageus/Dig/issues/16). Фактическая licensed Unity verification продолжается у отдельного approved owner [#511](https://github.com/bageus/Dig/issues/511) и не считается выполненной до `verified` evidence manifest.
+
 ## 1. Назначение
 
 Roadmap задаёт порядок реализации систем. GitHub Issues являются рабочими задачами, а этот документ — стабильной картой этапов и критериев перехода между ними.
@@ -33,7 +35,8 @@
 | [#13](https://github.com/bageus/Dig/issues/13) | Сохранения, версии и миграции |
 | [#14](https://github.com/bageus/Dig/issues/14) | Presentation, ввод, UI и диагностика |
 | [#15](https://github.com/bageus/Dig/issues/15) | Тесты, performance budgets и CI |
-| [#16](https://github.com/bageus/Dig/issues/16) | Верхнеуровневое отслеживание roadmap |
+| [#16](https://github.com/bageus/Dig/issues/16) | Завершённая базовая roadmap |
+| [#511](https://github.com/bageus/Dig/issues/511) | Licensed Unity runtime verification и release evidence |
 
 ## 4. Этап 0 — Архитектурный фундамент
 
@@ -43,7 +46,7 @@
 
 - [#1](https://github.com/bageus/Dig/issues/1);
 - [#2](https://github.com/bageus/Dig/issues/2);
-- [#15](https://github.com/bageus/Dig/issues/15).
+- [#15](https://github.com/bageus/Dig/issues/15) — repository quality/CI implementation; licensed Unity execution перенесён в [#511](https://github.com/bageus/Dig/issues/511).
 
 Результаты:
 
@@ -69,7 +72,7 @@
